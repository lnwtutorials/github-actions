name: test1-docker
on: [push]

jobs:
    docker-job:
        #runs-on: ubuntu-latest
        runs-on: self-hosted
        container:
            image: nginx
            #credentials:
                #username: ${{secrets.DOCKER_USER}}
                #password: $DOCKER_PASS
            ports:
                - 80
            volumes:
                - test_vol:/usr/local/nginx/html
            env:
                T1: "Hello World"
        steps:            
            - name: test variable
              run: echo Hello
            - name: test variable
              run: |
                echo ${{secrets.DOCKER_USER}}
                echo ${{env.T1}}

    ubuntu-sleeper:
        runs-on: lab-runner
        container: 
            image: amd64/busybox
            env:
                T1: Test
            ports:
                - 80
        steps:
            - name: container in steps
              uses: docker://amd64/busybox:stable-uclibc
              with:
                entrypoint: echo
                args: echo hello

            - name: environment test
              run: |
                echo $T1
                echo ${{env.DOCKER_USER}}
                echo ${{env.DOCKER_USER.DOCKER_PASS}}
            
        
        